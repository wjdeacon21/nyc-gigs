# ===========================================
# Concert Notify Configuration
# ===========================================

# Spotify OAuth Credentials (required)
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Server Configuration
PORT=8888
NODE_ENV=development

# OAuth Redirect URI (must match Spotify dashboard settings)
REDIRECT_URI=http://localhost:8888/callback

# ===========================================
# Show Data Sources
# ===========================================

# Local file path (primary - written by scraper)
# SHOWS_LOCAL_PATH=./data/shows.json

# Remote URL (fallback if local file doesn't exist)
# SHOWS_REMOTE_URL=https://raw.githubusercontent.com/wjdeacon21/scrapedShows/main/data/shows_recent.json

# ===========================================
# Scraper Configuration
# ===========================================

# Number of pages to scrape (default: 5)
# SCRAPER_PAGES=5

# Delay between page requests in ms (default: 1000)
# SCRAPER_DELAY_MS=1000

# Keep timestamped history files (default: false)
# SCRAPER_KEEP_HISTORY=false

# ===========================================
# Spotify Performance
# ===========================================

# Max pages of liked songs to fetch (50 songs/page)
# Set to 0 for unlimited, default is 10 (500 songs)
# SPOTIFY_MAX_LIKED_PAGES=10

# ===========================================
# Logging
# ===========================================

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info
